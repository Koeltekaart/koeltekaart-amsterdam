services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      # Optional: set a secret to protect the heat plan toggle API
      # - HEAT_PLAN_SECRET=your-secret-here
    restart: unless-stopped
